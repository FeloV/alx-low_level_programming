#include "main.h"

/**
 *
 * _abs - computes the absolute valueof an integer
 * @i: input number as an integer
 *
 * Return: absolute value
 */
int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (-1 * i);
	}
}
